# Copy this file to .env to override the defaults.
# Dev default: http://localhost:5000/api   Production build default: the hosted Render API
VITE_API_URL=http://localhost:5000/api
# Optional: only needed if the Socket.IO server is on a different host than the API
# VITE_SOCKET_URL=http://localhost:5000
